import { Component } from '@angular/core';

/** Placeholder so Members can activate `/admin` without a child area. */
@Component({
  selector: 'vc-admin-landing',
  standalone: true,
  template: '',
})
export class AdminLandingPage {}
